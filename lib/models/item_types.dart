import 'package:flutter/material.dart';
import 'package:inspector/models/item.dart';

class Totem extends Item {
  Totem({
    required super.name,
    super.type = ItemType.totem,
    required super.imageURL,
  });
}

class Ring extends Item {
  Ring({
    required super.name,
    super.type = ItemType.ring,
    required super.imageURL,
  });
}

class Pocket extends Item {
  Pocket({
    required super.name,
    super.type = ItemType.pocket,
    required super.imageURL,
  });
}

class Pendant extends Item {
  Pendant({
    required super.name,
    super.type = ItemType.pendant,
    required super.imageURL,
  });
}

class Weapon extends Item {
  Weapon({
    required super.name,
    super.type = ItemType.weapon,
    required super.imageURL,
  });
}

class Belt extends Item {
  Belt({
    required super.name,
    super.type = ItemType.belt,
    required super.imageURL,
  });
}

class Hat extends Item {
  Hat({
    required super.name,
    super.type = ItemType.hat,
    required super.imageURL,
  });
}

class Face extends Item {
  Face({
    required super.name,
    super.type = ItemType.face,
    required super.imageURL,
  });
}

class Eye extends Item {
  Eye({
    required super.name,
    super.type = ItemType.eye,
    required super.imageURL,
  });
}

class Top extends Item {
  Top({
    required super.name,
    super.type = ItemType.top,
    required super.imageURL,
  });
}

class Bottom extends Item {
  Bottom({
    required super.name,
    super.type = ItemType.bottom,
    required super.imageURL,
  });
}

class Shoe extends Item {
  Shoe({
    required super.name,
    super.type = ItemType.shoe,
    required super.imageURL,
  });
}

class Earring extends Item {
  Earring({
    required super.name,
    super.type = ItemType.earring,
    required super.imageURL,
  });
}

class Shoulder extends Item {
  Shoulder({
    required super.name,
    super.type = ItemType.shoulder,
    required super.imageURL,
  });
}

class Glove extends Item {
  Glove({
    required super.name,
    super.type = ItemType.glove,
    required super.imageURL,
  });
}

class Android extends Item {
  Android({
    required super.name,
    super.type = ItemType.android,
    required super.imageURL,
  });
}

class Emblem extends Item {
  Emblem({
    required super.name,
    super.type = ItemType.emblem,
    required super.imageURL,
  });
}

class BadgeItem extends Item {
  BadgeItem({
    required super.name,
    super.type = ItemType.badge,
    required super.imageURL,
  });
}

class Medal extends Item {
  Medal({
    required super.name,
    super.type = ItemType.medal,
    required super.imageURL,
  });
}

class Secondary extends Item {
  Secondary({
    required super.name,
    super.type = ItemType.secondary,
    required super.imageURL,
  });
}

class Cape extends Item {
  Cape({
    required super.name,
    super.type = ItemType.cape,
    required super.imageURL,
  });
}

class Heart extends Item {
  Heart({
    required super.name,
    super.type = ItemType.heart,
    required super.imageURL,
  });
}
